/*
 * Basic Nextflow config for running fastp
 */

params {
    // Input files
    reads1 = "data/R1.fastq.gz"
    reads2 = "data/R2.fastq.gz"
    sample = "sample"   // sample name prefix
}

process {
    executor = 'local'
    cpus = 4
    memory = '8 GB'
}

